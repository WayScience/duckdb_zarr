# README Draft Notes

The next public-facing README should center on:

- relational SQL access to Zarr metadata and chunk layout
- the path from metadata discovery to `zarr_cells()`
- developer commands: `make`, `make fixture`, `make test_metadata`
- clear statement that Phase 1 is metadata-first and local-only
